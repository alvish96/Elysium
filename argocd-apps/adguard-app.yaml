apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: adguard-home
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/alvish96/Elysium.git'
    targetRevision: HEAD
    path: app-code/adguard 
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: networking
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
