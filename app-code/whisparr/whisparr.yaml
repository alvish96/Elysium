apiVersion: v1
kind: PersistentVolume
metadata:
  name: whisparr-config-pv
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual-whisparr
  local:
    path: /mnt/internal/appdata/whisparr/config
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - atlantis
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisparr-config-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual-whisparr
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whisparr
  namespace: media
  labels:
    app: whisparr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: whisparr
  template:
    metadata:
      labels:
        app: whisparr
    spec:
      nodeSelector:
        kubernetes.io/hostname: atlantis

      dnsPolicy: ClusterFirst
      dnsConfig:
        nameservers:
          - 8.8.8.8
          - 1.1.1.1
        options:
          - name: ndots
            value: "1"

      securityContext:
        seccompProfile:
          type: Unconfined
        appArmorProfile:
          type: Unconfined

      containers:
        - name: whisparr
          image: ghcr.io/hotio/whisparr:latest
          securityContext:
            allowPrivilegeEscalation: true
          env:
            - name: PUID
              value: "0"
            - name: PGID
              value: "0"
            - name: TZ
              value: "Asia/Kolkata"
          ports:
            - containerPort: 6969
              name: http
          volumeMounts:
            - name: config
              mountPath: /config
            - name: nexus-pool
              mountPath: /data
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: whisparr-config-pvc
        - name: nexus-pool
          hostPath:
            path: /mnt/nexus_pool
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: whisparr-svc
  namespace: media
spec:
  selector:
    app: whisparr
  type: NodePort
  ports:
    - name: http
      port: 6969
      targetPort: 6969
      nodePort: 30469
